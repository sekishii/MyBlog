node {
    stage('Checkout') {
        checkout scm
    }
    stage('Build') {
        echo 'build'
    }
    stage('Test') {
        echo 'test'
    }
    stage('Deploy') {
        echo 'deploy'
    }
}